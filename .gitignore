# ===================== 操作系统 =====================
# Mac
.DS_Store
.AppleDouble
._*
# Windows
Thumbs.db
desktop.ini
*.lnk
# Linux
*~

# ===================== 通用 IDE / 编辑器 =====================
# JetBrains 系列（IntelliJ IDEA / GoLand / CLion / PyCharm / WebStorm / PhpStorm / Rider 等）
.idea/
*.iml
*.ipr
*.iws
out/
# Rider（.NET）：解决方案级用户设置
*.DotSettings.user

# vscode
.vscode/
*.code-workspace

# Sublime
*.sublime-project
*.sublime-workspace
# Vim
*.swp
*.swo
# Atom
.atom/

# ===================== 其他 IDE / 编辑器（小众） =====================
# Eclipse
.metadata/
.settings/
.classpath
.project
/bin/
# NetBeans
nbproject/private/
nbdist/
.nb-gradle/
# Xcode
xcuserdata/
DerivedData/
*.xcuserstate
*.xccheckout
*.moved-aside
*.dSYM/
.swiftpm/
# Android Studio
local.properties
*.apk
*.aab
*.ap_
captures/
.externalNativeBuild/
.cxx/
# Visual Studio / .NET
.vs/
*.suo
*.user
*.userosscache
*.ncb
*.sdf
*.opensdf
*.cachefile
*.VC.db
*.VC.VC.opendb
/obj/
# Qt Creator
*.pro.user
*.pro.user.*
.qmake.stash
.qmake.cache
.qtc_clangd/
*.autosave
# Code::Blocks
*.layout
*.depend
# RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
# Spyder
.spyproject/
# BlueJ
bluej.pkh
# Komodo Edit/IDE
.komodotools/

# ===================== AI 开发工具 =====================
# OpenCode
.opencode/
.opencode.json
opencode.json
opencode.jsonc
opencode-cache/
.opencode-snapshots/
.opencode-history/
*.opencode.tmp

# Claude Code
.claude/
.claudeignore
.claude-history/
.claude/conversations/
.claude/sessions/
.claude/checkpoints/
.claude/skills/.cache/
.claude/settings.local.json
*.skill-backup
conversation-history.jsonl

# OpenAI Codex
.codex/
.codexignore
codex_history/
codex-*.log

# GitHub Copilot
.copilot/
.copilot-agent/
copilot-*.log
.copilotignore
.vscode/copilot/

# Cursor AI
.cursor/
.cursor-index/
.cursor-cache/

# Codeium / Windsurf
.codeium/
windsurf-sessions/

# Tabnine
.tabnine/
.tabnine-cache/

# Gemini CLI
.gemini/
.geminiignore
gemini-*.log

# Cline / Roo
.cline/
.clineignore
.roo/
.rooignore

# Aider
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v3
.aider.models.cache.v3

# Continue
.continue/
.continueignore

# AI 通用临时文件
ai-tmp/
ai-generated-temp/
*.ai-backup

# ===================== 前端 / Node.js =====================
# 依赖
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lockb
bun.lock

# 构建产物
dist/
build/
output/
coverage/

# 日志、缓存
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm
.eslintcache

# 编译产物
*.tsbuildinfo
*.js.map
*.d.ts.map

# 框架级产物
.next/
.nuxt/
.output/
.svelte-kit/
.vercel/

# ===================== Python =====================
__pycache__/
*.py[cod]
*$py.class
.Python
env/
venv/
.venv/
env.bak/
venv.bak/
.pytest_cache/
.coverage
htmlcov/
*.pyd

# ===================== Java / Maven / Gradle =====================
# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
# Gradle
.gradle/
gradle-app.setting
!gradle-wrapper.jar
# 编译 class
*.class
*.jar
*.war

# ===================== JVM / 调试产物 =====================
# 堆转储与 JVM 崩溃日志（jmap/hprof 堆转储、HotSpot 致命错误日志与崩溃重放文件）
*.hprof
hs_err_pid*.log
replay_pid*
heapdump.*
*.dmp
# 调试转储目录与文件（dump/ 全局排除；项目内无 dump 源码目录，已核实）
dump/
*.dump
# Derby 嵌入式数据库调试日志（与已有 *.log 规则双保险）
derby.log

# ===================== 构建 / 测试中间产物 =====================
# Maven 插件级独立中间产物（target/ 已覆盖常规产物，以下为 target/ 之外的兜底预防）
# Flatten 插件扁平化 POM（*.flattened-pom.xml）、依赖解析失败标记（*.lastUpdated）
*.flattened-pom.xml
*.lastUpdated
# Shade 插件简化 POM（官方默认生成在模块根目录 ${basedir}，必须忽略）
dependency-reduced-pom.xml
# Compiler 插件增量编译状态（maven-status/，兜底预防）
maven-status/
# 测试报告目录（target 之外独立输出时兜底，均带尾斜杠只匹配目录）
surefire-reports/
test-output/
test-results/
# API 接口测试中间文件（精确模式；scripts/API-TEST 下 .py/.ps1 为应入库测试脚本，禁止整目录忽略）
scripts/API-TEST/*.tmp
scripts/API-TEST/*.token.json

# ===================== C/C++ / CMake =====================
cmake-build-*/
cmake-build-debug/
cmake-build-release/
Debug/
Release/
*.o
*.obj
*.exe
*.dll
*.so
*.a
*.lib

# ===================== Rust =====================
# 编译产物（/target/ 仅匹配根目录，与 Maven 的 target/ 不冲突）
/target/
*.rs.bk
# Cargo.lock：库项目通常忽略，应用项目建议提交
Cargo.lock

# ===================== Go =====================
# 测试二进制与覆盖率输出
*.test
*.out
*.prof
coverage.out
# 编译产物（macOS 动态库）
*.dylib

# ===================== PHP =====================
# Composer 依赖
/vendor/
composer.phar
# PHPUnit 结果缓存
.phpunit.result.cache

# ===================== Dart / Flutter =====================
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies

# ===================== 客户端构建产物（deploy/cloudoffice-flutter-app） =====================
# 客户端最终产物（Web 部署包、Windows Release 产物）统一输出至 deploy/cloudoffice-flutter-app，
# 与后端 jar（*.jar）策略一致：产物不入库，仅保留 .gitkeep 占位文件保证目录结构可追踪。
# 注意：必须带 deploy/ 路径前缀，避免误伤 cloudoffice-flutter-app/web、windows 客户端源码目录。
deploy/cloudoffice-flutter-app/web/*
!deploy/cloudoffice-flutter-app/web/.gitkeep
deploy/cloudoffice-flutter-app/windows/*
!deploy/cloudoffice-flutter-app/windows/.gitkeep

# ===================== 数据库 / 缓存 / 日志 / 临时 =====================
# 日志
*.log
logs/
log/
# 部署与调试过程临时/中间文件（进程 PID、错误日志、调试工作区）
*.err
*.pid
work/
# 数据库
*.db
*.sqlite
*.sqlite3
# 缓存、临时文件
tmp/
temp/
.cache/
# 本地分支记录、补丁、合并冲突备份
*.patch
*.diff
*.orig
# 备份文件
*.bak
*.backup

# ===================== 工具残留 =====================
# API 调试 / 抓包工具会话文件（Fiddler *.saz、Charles *.chls、HTTP Archive *.har）
*.saz
*.chls
*.har
# 编辑器 / 终端会话与历史、调试跟踪输出（项目内无同名源码扩展名，已核实）
*.history
*.session
*.trace

# ===================== 环境密钥 =====================
.env
.env.local
.env.dev
.env.test
.env.staging
.env.prod
.env.*
!.env.example
# 项目特有敏感文件（密钥、环境配置，一律不提交）
keys/
env.json
# 旧文档副本目录（重新初始化后不纳入版本库）
docs2/

# ===================== 包管理器 =====================
.pnp
.pnp.js

# ===================== 压缩包 =====================
*.tgz

# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 CloudStrolling/jenemy8023 <jenemy8023@163.com>
